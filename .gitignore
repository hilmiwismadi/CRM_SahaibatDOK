# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
dist/
build/

# env
.env
.env.local
.env.*.local

# wa-bridge session state (never commit auth keys)
apps/wa-bridge/auth_state/
wa-session/

# logs
npm-debug.log*
*.log

# misc
.DS_Store
*.pem
.vscode/*.log

# prisma
apps/web/prisma/dev.db

# scrape output — contains real (scraped, PII-adjacent) data, regenerable
scrape-output/

# JSON DB backups (scripts/backup-json.ts) — real lead/contact/message data,
# never commit. Copy this off-machine yourself; git is not the backup.
apps/web/backups/

# ad-hoc pg_dump / auth_state backups made before local dev restarts —
# real lead/message data + WA session keys, never commit.
/backups/

# typescript
*.tsbuildinfo
